language: php

php:
    - '7.1'
    - '7.2'

sudo: false

before_script:
    - cp .env.travis .env
    - composer self-update
    - composer install --prefer-dist --no-suggest

script: vendor/bin/phpunit
